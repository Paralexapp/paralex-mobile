import 'package:flutter/material.dart';

class ParalegalHome extends StatelessWidget {
  const ParalegalHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text(" No news available here yet")),
    );
  }
}
